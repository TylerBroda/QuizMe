import 'package:flutter/material.dart';

class PeersScreen extends StatelessWidget {
  const PeersScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Container(child: Text("peers"),
          
        ),
      ),
    );
  }
}