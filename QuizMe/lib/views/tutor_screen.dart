import 'package:flutter/material.dart';

class TutorScreen extends StatelessWidget {
  const TutorScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Container(child: Text("tutor map"),
          
        ),
      ),
    );
  }
}